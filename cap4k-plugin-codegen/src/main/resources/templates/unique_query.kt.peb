package {{ basePackage }}{{ templatePackage }}{{ package }}
{{ imports() }}

/**
 * 该文件由 [cap4k-ddd-codegen-gradle-plugin] 生成
 * @author cap4k-ddd-codegen
 */
object {{ Query }} {

    class Request(
    {%- for p in RequestParams %}
        val {{ p.name }}: {{ type(p.type) }},{% endfor %}
        val {{ ExcludeIdParamName }}: {{ type(IdType) }}?
    ) : RequestParam<Response>

    class Response(
        val exists: Boolean
    )
}
